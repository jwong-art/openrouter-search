{
  "id": "openrouter-search",
  "name": "@jwongart/openrouter-search",
  "description": "Provides independent OpenRouter search tools for Perplexity web search and Grok X Search. These tools consume OpenRouter credits and can work independently of OpenClaw's built-in web_search.",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "properties": {
      "timeout_ms": {
        "type": "integer",
        "minimum": 15000,
        "maximum": 300000,
        "default": 120000,
        "description": "Request timeout in milliseconds. Default is 120000, maximum is 300000."
      }
    },
    "additionalProperties": false
  },
  "activation": {
    "onStartup": true
  },
  "contracts": {
    "tools": [
      "openrouter_perplexity_search",
      "openrouter_x_search"
    ]
  }
}
